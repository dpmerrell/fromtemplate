[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "__FILE_NAME__"
requires-python = ">= 3.10"
version = "0.0.1"

[project.scripts]
__FILE_NAME___script = "__FILE_NAME__:__FILE_NAME___script"

[tool.setuptools]
include-package-data = true

